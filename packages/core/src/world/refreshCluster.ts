import type { NavGraph } from "../graph";
import type { TileCoord, TileGrid } from "../grid";

export function refreshRoadCluster(_grid: TileGrid, _graph: NavGraph, _coord: TileCoord) {
	// TODO
}

export function refreshWalkCluster(_grid: TileGrid, _graph: NavGraph, _coord: TileCoord) {
	// TODO
}
